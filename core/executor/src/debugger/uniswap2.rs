use super::*;

#[test]
fn test_uniswap2() {
    let mut _debugger = EvmDebugger::new();
	// let tx = Transaction {};
	// let stx = mock_signed_tx(tx);
	// let resp = debugger.exec(1, vec![stx]);
    clear_data();
}
