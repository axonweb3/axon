coverage:
  status:
    project:
      default:
        target: 50
        threshold: null
    patch: false

comment: 
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: false 
  require_base: false    
  require_head: true    
